# 图表选择

- 时间序列、健康指数、日用气量变化：`line`。
- 企业排名、风险数量、不同设备对比：`bar`。
- 少量互斥类别构成且总量有意义：`pie`，类别超过6个时改用柱状图。
- 两个连续数值的关系：`scatter`。

规则：

- 标题写清指标和时间。
- `unit` 必填，确无单位时填“无量纲”或“户/次”。
- x轴字段和 y轴字段必须来自同一 `source_query_id`。
- 缺失值保持空值；不得补成0。
- 排名柱状图 SQL 先排序并限制合理数量。
- 图表用于解释关系，不重复制造与正文无关的装饰图。
